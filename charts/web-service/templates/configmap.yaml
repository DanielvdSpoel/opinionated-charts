apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "web-service.fullname" . }}-config
  labels:
    {{- include "web-service.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
